export const polygon_data = {};

export const pushToSidebar = (key) => {
  polygon_data[key] = { key, id: "", type: "" };

  console.log(polygon_data);
};

// jQuery(document).ready(function ($) {
//   let object = {};
// });
